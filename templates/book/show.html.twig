{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
   <center> <h1>Book</h1></center>
<div class="container">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book.name }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ book.theme }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ book.dateCreation }}</td>
            </tr>
        </tbody>
    </table>

    <table><tbody>
  <tr> <td> <a href="{{ path('book_edit', {'id': book.id}) }}"  class="btn btn-info">edit</a> </td><td>
    {{ include('book/_delete_form.html.twig') }}
</td><tr>
</tbody></table>
<br><a href="{{ path('book_index') }}">back to list</a>

      
{% endblock %}
